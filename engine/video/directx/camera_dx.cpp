#include "stdafx.h"

#ifdef VIDEO_DIRECTX
#include "camera_dx.h"

#include "video_system_dx.h"

using namespace engine;

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

CameraDX::CameraDX()
:	pos_(0.0f, -5.0f, 0.0f),
	lookAt_(0.0f, 0.0f, 0.0f),
	up_(0.0f, 1.0f, 0.0f)
		 {}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

bool CameraDX::update(const float /*dt*/) {
	D3DXMatrixLookAtLH(&view_, &pos_, &lookAt_, &up_);
	DEBUG_ONLY(HRESULT res = )
		VideoDX::get().device().SetTransform(D3DTS_VIEW, &view_);
	assert(res == D3D_OK);

	return true;
}

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif