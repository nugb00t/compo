#ifndef SYNC_INCLUDED
#define SYNC_INCLUDED

#include "utility/callable.h"

namespace engine { namespace sync {

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

const TCHAR EXIT_SIGNAL_NAME[] = _T("COMPONENTS_EXIT_SIGNAL");

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// logic

const float LOGIC_TICKRATE = 20.f;
const unsigned LOGIC_FRAMETIME = static_cast<unsigned>(1000.f / LOGIC_TICKRATE);

struct Desire {
	Vector3 velocity;
	Vector3 aim;
	unsigned state;
};

struct LogicUpdate {
	std::vector<Desire> desires;
	unsigned age;
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
// video

const float VIDEO_TICKRATE = 60.f;
const unsigned VIDEO_FRAMETIME = static_cast<unsigned>(1000.f / VIDEO_TICKRATE);

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

}}

#endif
