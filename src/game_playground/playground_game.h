#pragma once

#include "game.h"

namespace game_playground {

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class PlaygroundGame : public engine::Game {
public:
	enum Entities {
		ENTITY_ORB,
		ENTITY_BALL,

		MAX_ENTITIES
	};

	enum Screens {
		SCREEN_THREAD_PROFILER,
		SCREEN_HUD,

		MAX_SCREENS
	};

public:
	PlaygroundGame();
};

///////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

}